spring:
  datasource:
    url: jdbc:h2:mem:librarydb
  sql:
    init:
      mode: always

  jpa:
      generate-ddl: true
      hibernate:
        ddl-auto: create

      properties:
        hibernate:
          format_sql: false

      show-sql: true

      defer-datasource-initialization: true