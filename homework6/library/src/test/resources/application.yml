spring:
  datasource:
    url: jdbc:h2:mem:librarydb
  sql:
    init:
      mode: always
      data-locations: data.sql

  jpa:
      generate-ddl: true
      hibernate:
        ddl-auto: create

      defer-datasource-initialization: true